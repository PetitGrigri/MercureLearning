{% if topic|default('') is not empty and id|default('') is not empty %}
  <article class="box content">
    <p>
      <strong>{{ topic }}</strong>
      (<a href="{{ path('discover_book', {'id': id}) }}">jsonld</a> |
      <a href="{{ path('get_book', {'id': id}) }}">API</a>)
    </p>
    <p>
      État :
      <strong id="book-{{ id }}-status" data-book-source="{{ mercure(topic) }}">
        Unknown
      </strong>
    </p>
    <div class="buttons has-addons">
      <button
        class="button is-danger"
        data-controller="button"
        data-target="{{ path('book_status', { 'id': id, 'status': 'out-of-stock' }) }}"
      >
        Out of stock
      </button>
      <button
        class="button is-success"
        data-controller="button"
        data-target="{{ path('book_status', { 'id': id, status: 'in-stock' }) }}"
      >
        In Stock
      </button>
    </div>
  </article>
{% endif %}
